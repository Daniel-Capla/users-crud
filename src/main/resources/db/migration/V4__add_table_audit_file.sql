CREATE TABLE audit_file
(
    id         INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    json       VARCHAR(255),
    created_at TIMESTAMP WITHOUT TIME ZONE,
    CONSTRAINT pk_auditfile PRIMARY KEY (id)
);

ALTER TABLE users
    ADD role SMALLINT;